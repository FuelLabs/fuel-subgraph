{
  "name": "ars",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined ars/ars",
    "create-local": "graph create --node http://localhost:8020/ ars/ars",
    "remove-local": "graph remove --node http://localhost:8020/ ars/ars",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ars/ars",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
